# Bug Reproduction

## 问题现象

流水线为阶段错误补充上下文时使用普通格式化，破坏了底层领域错误的错误链。上层无法再通过领域错误解析识别具体错误码，导致错误分类和后续响应处理失真。

## 复现方式

在项目根目录执行：

```bash
go test ./internal/service -count=1 -run '^TestBug003_StageErrorKeepsDomainType$'
```

测试构造内容与文件类型不匹配的输入，使校验阶段返回领域错误，再从流水线返回值中读取错误码。

## 基线结果

原始基线代码返回的错误不再保留可解包的领域错误，解析出的错误码不是 `MAGIC_MISMATCH`，测试失败。

## 预期结果

阶段上下文在保留底层错误链的前提下返回，上层能够识别原始领域错误码，测试正常通过。
