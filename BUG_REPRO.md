# Bug Reproduction

## 问题现象

任务处理器在进入流水线前剥离了调用方 context 的取消信号。即使上游已经取消任务，下游扫描请求仍会继续等待，导致任务停止不及时并占用工作协程。

## 复现方式

在项目根目录执行：

```bash
go test ./internal/worker -count=1 -run '^TestBug002_ProcessorPropagatesCancellation$'
```

测试先取消传入处理器的 context，再调用包含慢速扫描请求的处理流程，并检查处理器是否及时返回。

## 基线结果

原始基线代码不会把取消状态传给流水线，处理过程会等待扫描请求结束，测试因耗时超过限制而失败。

## 预期结果

处理器沿调用链传递原始 context；取消发生后，下游请求立即结束，处理器在限制时间内返回。
